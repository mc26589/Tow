name: Core Web Vitals Analytics Loop

on:
  schedule:
    - cron: '0 1 * * *' # Runs daily at 1:00 AM UTC
  workflow_dispatch:

permissions:
  contents: write

jobs:
  analyze:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Analyze Vitals & Suggest Code snippet
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: npm run agent:analytics
        
        # Optional: Auto-apply the AI suggestion if safe
      # - name: Auto-apply technical improvements (LCP/INP/CLS)
      #   run: |
      #     npm run agent:apply-fixes
      #     git config --global user.name "AI-Towing-Vitals"
      #     git config --global user.email "vitals@towing.haifa.ai"
      #     git add src/app/ src/components/ || true
      #     git commit -m "Auto-fix Core Web Vitals (LCP/INP/CLS) [skip ci]" || echo "No fixes applied"
      #     git push
